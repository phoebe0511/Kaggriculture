{
  "name": "cash-sweep",
  "note": "現金底線的兩個參數。A 固定用 ref-v1，每個對手只跟現在的預設差一個值。cash_reserve_days = 日常開銷(雇工+飼料+種子)要留幾天份；seed_buffer_days = 種子存幾天份。 ⚠️ 2026-08-21 更新：(1) `agents/gen1.py` 併入 `agents/gen0.py`，`gen0.DEFAULT_PARAMS` 多了 gen1 的 21 項（n_structures 9->12、optimal_assignment False->True、tiles_per_unit、water_on_demand 等）。**這個 sweep 內部仍然可比**（所有變體共用同一組新預設），但**不能拿現在的數字跟 2026-08-16~19 那批比**。 (2) Gen0 線的 9 個對手已刪除（ref-v1 / ref-v2 / gen0 / 5 個 gen0-* / pq4）。固定 A 方請改用 `ref-v11`（唯一把 51 項參數完整展開的，不會隨預設值飄）。",
  "opponents": [
    {
      "name": "crd-1",
      "entry": "agents.gen0:act",
      "params": {
        "cash_reserve_days": 1
      }
    },
    {
      "name": "crd-2",
      "entry": "agents.gen0:act",
      "params": {
        "cash_reserve_days": 2
      }
    },
    {
      "name": "crd-3",
      "entry": "agents.gen0:act",
      "params": {
        "cash_reserve_days": 3
      }
    },
    {
      "name": "crd-5",
      "entry": "agents.gen0:act",
      "params": {
        "cash_reserve_days": 5
      }
    },
    {
      "name": "crd-8",
      "entry": "agents.gen0:act",
      "params": {
        "cash_reserve_days": 8
      }
    },
    {
      "name": "sbd-1",
      "entry": "agents.gen0:act",
      "params": {
        "seed_buffer_days": 1
      }
    },
    {
      "name": "sbd-2",
      "entry": "agents.gen0:act",
      "params": {
        "seed_buffer_days": 2
      }
    },
    {
      "name": "sbd-4",
      "entry": "agents.gen0:act",
      "params": {
        "seed_buffer_days": 4
      }
    },
    {
      "name": "sbd-6",
      "entry": "agents.gen0:act",
      "params": {
        "seed_buffer_days": 6
      }
    }
  ]
}
