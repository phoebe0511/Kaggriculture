# 2026-08-18：Gen1 第一、二輪優化

## 第一輪量尺

第一輪需求感知版凍結為 `config/opponents/ref-v7.json`：12 格動物建物、對手產能
折算、內部飼料需求、草莓高需求門檻、前半季 COOP 上限。正式第二輪一律對這份
完整參數快照做 paired seeds 比較。

## 第二輪採用項目

1. **肥料貨幣 ROI**：只對 ongoing 作物施肥，且未來三天的額外產品價值至少要
   等於直接賣掉肥料的價值。60 局對 ref-v7 為 46 勝 14 負，平均 `+$3,311`。
2. **保守季末縮編**：剩 2 天最多 10 hands、最後一天最多 8 hands。從剩 6 天
   就降到 11 的版本較差；保守版與 ROI 合併後再多約 `$150`。
3. **全域最短任務配對**：每個優先序層使用純 Python Hungarian assignment；
   PLANT 因種子原子驗證維持逐筆分派。直接對打舊貪婪分派 60 局為 58 勝 2 負，
   平均 `+$8,686`，買地仍固定 day 9 / day 11。

固定象限 zoning 沒有重啟；歷史消融已證實它增加移動。本輪改善的是 unit 與任務
的整體配對，不是分區。

## 拒絕項目

在上述最佳組合上重掃動物建物格數，12 格分別擊敗 13 / 14 / 15 格；24 局平均
現金差為 `+$6,264 / +$11,997 / +$12,771`，所以維持 12 格。

## 最終驗證

- `pytest`：33 passed。
- 30 paired seeds 對 starter：60 勝 0 負，平均 `$111,348 vs $3,581`。
- 30 paired seeds 對 ref-v7：60 勝 0 負，平均 `$88,616 vs $77,217`，
  差 `+$11,398`。
- baseline ladder：10 個對手 × 10 局，共 100 勝 0 負。
- MOVE 從第一輪約 `59.1%` 降到 `56.6%`。

## 評估器修正

凍結 JSON 原本仍會先合併目前的 Gen1 defaults，導致新增預設後舊 ref 偷吃到新
策略。`eval.runner` 現在替含 `frozen` 的完整快照加上 replace 標記，Gen0 / Gen1
入口會完全以快照取代 live defaults；ref-v7 的 SHA-256 仍保持不變。第二輪完整
參數凍結為 `config/opponents/ref-v8.json`。
