# 2026-08-31

## §52 未解問題：PPO 會把好策略越練越差

### 52.1 現象

給 PPO 一個好起點（`ckpt-cma1-round0-tgt`，hybrid market 在 ladder 14.1%），
訓練越久 greedy 表現越差。三次跑都是同一個形狀：

```
                    第 10 輪    之後
ppo-cma1-market     77,628     -> 第 300 輪 53,812，best.pt 停在第 10 輪
ppo-cma1-fixed      46,490     -> 第 40 輪 30,317（KL 護欄修好後跑的）
ab-own（跑著）       69,442     -> 取樣現金第 14 輪已掉到 41,210
```

不是「學不動」（那是從零那次，現金恆為 0、§47），是**本來好的被練壞**。

### 52.2 已排除

**步伐太大 —— 排除。** KL 護欄有 bug（看累積平均而非當前 epoch，
commit `144f8cb`），修好之後 `epochs_done` 仍然全是 8，代表逐 epoch KL
從來沒碰到 `1.5 × 0.02 = 0.03`。`ab-own` 第 14 輪的 KL(末 epoch) 是 **0.0118**
—— 只有門檻的 40%，策略每輪移動很小，照樣往壞的方向走。

**走得慢一樣會走到錯的地方。所以問題在方向，不在步長。**

### 52.3 還沒排除的候選

```
1. reward 對齊
   目標是「期末誰的錢多」，但逐步 reward 是「自己的錢增加多少」，
   期末勝負只有 ±1.0 且被折扣到 11.6%（0.997^718），比現金項小 26 倍。
   -> A/B 跑著（ab-own vs ab-zs，除 reward 外完全相同，同 --seed）

2. 取樣 vs greedy 分家
   PPO 優化的是取樣版，上場用 greedy。§44 直接量到兩者反向：
   取樣 12,999 -> 31,772（升），同一段訓練 greedy 44,060 -> 30,740（降）。
   但這次連取樣現金也在掉（60,000 -> 41,210），所以它不能單獨解釋。

3. advantage 估得不對
   ev 0.98 看起來很好，但那是在現在這個 reward 下量的。
   value head 可能學到「預測自己的現金軌跡」而不是「預測勝負」。

4. 別的（還沒想到的）
```

### 52.4 🔴 方法學：先前的跨run比較都不可靠

動作取樣吃全域 RNG，pool 的 worker 沒有 seed torch（commit `528e692`）。
實測：同一個 checkpoint、除 `--out` 外參數完全相同，跑兩次

```
it 0 現金        63,543  vs  60,704
第 10 輪 greedy  77,628  vs  46,490     差 4.4 個評估標準誤
```

而且舊寫法下 **`--seed0` 對動作取樣根本沒有作用**（反證測試證明的）。

**後果**：凡是「比較兩次訓練跑」得出的結論都要打折 ——
§48 的「+11,402 t=+14.6」、§50.2 的「第 10 輪最好」都是單次跑。
「會崩」這件事有 3/3 支持，但幅度與細節在 run-to-run 變異範圍內。

**不受影響**：ladder 那些 1,280 局的量測（那是在量固定的 checkpoint）。

### 52.5 順帶修掉的記錄缺陷

`greedy_cash` / `greedy_win` 本來在 `json.dumps(row)` **之後**才設，
所以從沒進過 `train.jsonl`，只留在 stdout —— 每次分析都得用 regex 剖 log。
已修，並加 `--watch`（commit `496a1b3`）：

    python -m model.ppo_train --watch model/artifacts/ab-own model/artifacts/ab-zs

### 52.6 判讀標準（先寫死）

`ab-own` vs `ab-zs`，各 100 輪，同 `--seed 4242 --seed0 900000`：

| 看到什麼 | 結論 |
|---|---|
| ab-zs 不崩、ab-own 崩 | reward 對齊是病因 |
| 兩組都崩 | 不是 reward，回去查候選 2 / 3 |
| ab-zs 的 ev 掉到 0.5 以下且也崩 | §37 的變異數擔憂成立，差值不可行，1 和它一起排除 |

### 52.7 現況

**出貨版仍是 `cma1-g50-wt`（ladder 24.1%），沒有任何網路版本贏過它。**
模仿追不上（§51：DAgger 四輪，純網路 6.6%），PPO 目前會把好策略練壞。

---

## §53 交接（11:15）

### 53.1 假設「advantage 幾乎都是雜訊」—— 已證偽

hybrid 模式下工人動作全是 gen0 出的，我猜網路只控制 market、對結果影響很小，
所以 `model/ppo.py:92` 的無條件正規化把雜訊放大成訊號。

量法（`scratchpad/control_probe.py`）：同一組地圖 seed、同一個 policy，
**只換動作取樣的 seed**。跑出來的差異完全來自網路自己的隨機選擇。

```
地圖 6 張 × 取樣 4 次
  網路自己的取樣造成的（可控）   sd 16,390   變異數 268,645,395   58.9%
  地圖／對手造成的（不可控）     sd 13,705   變異數 187,822,935   41.1%
```

**網路控制了 58.9% 的結果變異數。訊號很強。** 這個假設不成立，
§52.3 的候選 3（advantage 估得不對）要往別的方向想。

順帶：**單局現金對取樣的敏感度極高（sd 16,390）**。20 局／輪的平均 SE 約
3,665 —— 這對「用 10 局 greedy 挑 best.pt」是個警訊，那個 SE 約 5,200。

### 53.2 跑著的東西

`scratchpad/reward_ab.sh`（背景），A 組跑到第 28 輪：

```
ab-own（own-cash reward）
  第  9 輪 greedy  69,442
  第 19 輪 greedy  24,956     <- 照樣崩
  第 28 輪 取樣現金 26,090   ent 1.28   KL(末 epoch) 0.0119   ep 8
```

A 組約 11:50 跑完，接著自動跑 B 組（`--zero-sum`），約 12:50 全部完成。
兩組除了 reward 完全相同，都指定 `--seed 4242 --seed0 900000`。

看進度：

    python -m model.ppo_train --watch model/artifacts/ab-own model/artifacts/ab-zs

判讀標準寫在 §52.6。

### 53.3 今天修掉的（都有測試 + 反證）

```
144f8cb  target_kl 看累積平均而非當前 epoch  -> 護欄從沒生效
528e692  動作取樣吃全域 RNG                 -> 跨 run 比較都不可靠
496a1b3  greedy 沒寫進 train.jsonl；加 --watch 和逐 epoch KL
```

171 項測試通過。

### 53.4 未解的主問題

**PPO 會把好策略越練越差**（§52）。已排除：步伐太大、advantage 是雜訊。
還沒排除：reward 對齊（A/B 跑著）、取樣 vs greedy 分家。

### 53.5 出貨

**沒動過。`submission/cma1-g50-wt/`，ladder 24.1%。**
所有 commit 在 main，**沒有 push**。壓縮檔 `cma1-g50-wt.zip` 仍未追蹤。

---

## §54 🔴 找到了：reward 對齊就是病因。§37 的決定是錯的

### 54.1 A/B 結果

兩組除 reward 外完全相同，`--seed 4242 --seed0 900000`。seeding 修好
（`528e692`）之後**第 0 輪的取樣現金完全相同（63,239）**，對照是乾淨的。

```
              it0 取樣   後10輪取樣   後5次 greedy    ent 末   ev 末
ab-own        63,239      23,861        22,550        1.86    0.983
ab-zs         63,239      56,789        49,162        1.94    0.909
                          -62%          差 +26,612 合成SE 4,114 t=+6.5 顯著
                          -10%

輪       ab-own      ab-zs
  9      69,442      18,311
 19      24,956      19,949
 39      32,361      22,166
 59      22,149      59,223   <- ab-zs 高峰
 99      27,801      43,822
```

**own-cash 把策略毀掉，zero-sum 大致保住。** 符合 §52.6 預先寫死的第一條：
「ab-zs 不崩、ab-own 崩 -> reward 對齊是病因」。

### 54.2 §37 的理由被高估了

§37（08-28）把 reward 從零和改成只看自己，理由是對手項佔 86.4% 的變異數
「而且我們控制不了」。兩個前提都不成立：

- **「控制不了」是錯的** —— §49 的 sp100 量到：只改自己建物位置，
  對手現金動了 +11,158。
- **變異數的擔憂被高估** —— 第三條判讀標準（ev 掉到 0.5 以下）沒發生，
  ev 只從 0.983 掉到 0.909。value head 把多出來的變異數吸收掉了，
  而且 `contracts.py:516` 的 scalar 裡本來就有 `opp_money` / `money_ratio`。

§37 之後的**每一次 PPO 都用 own-cash**（warm3、ppo-hybrid、ppo-cma1-market、
ppo-scratch），也就是說 §48 / §50 / §52 觀察到的「越練越差」都可能是這個。

### 54.3 沒有解決的部分

zero-sum 不是萬靈丹：

```
ab-zs 高峰 59,223（第 59 輪）-> 末 43,822（第 99 輪）
```

高峰之後仍在下降，而且從沒回到起點附近（未訓練的 hybrid market
在 ladder 是 14.1% / 67,948）。所以 reward 對齊**是**病因之一，但不是全部。
§52.3 的候選 2（取樣 vs greedy 分家）還沒排除。

### 54.4 下一步

```
□ 把 --zero-sum 設成預設（改 model/ppo.py step_rewards 的預設值 + 文件）
□ ab-zs 的 best.pt 上 ladder 1,280 局，對 gen0 的 24.1%、
   對未訓練的 14.1% —— 這才知道有沒有真的變好
□ 用 zero-sum 重跑一次長的（300 輪），看高峰之後的下降是什麼
□ greedy 評估的局數要加 —— 單局現金對取樣的 sd 是 16,390，
   10 局的 SE 約 5,200，拿它挑 best.pt 偏噪（§53.1）
```
