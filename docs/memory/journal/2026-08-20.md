# 2026-08-20：behavior cloning 的錯誤是累乘的，資料量救不了

⚠️ **接手的人先看 §7。結論是：純模仿這條路走到底了，不是資料不夠、也不是接線壞掉。**

**起點**：昨天（08-19 §7d）記的「網路每一步 94% 猜對、整局打不出來」。今天把
`workflow.md` 那條線上剩下的兩個窟窿都補了 —— unit 的「目標」（v3）與市場（v4）
—— 每一步都有量測。**兩個都做出來了，兩個都沒有翻盤。**

最後量到一個把整件事定死的數字：老師的開局前 73 步是**逐字相同的腳本**
（60 個不同 seed），我們只跟得完 **22%**，而且第 6 步就掉出去。那個 6 不是意外，
是 `1 / (1 - 0.868)` 算出來的。細節在 §7。

---

## 1. 今天蓋了什麼

| 檔案 | 做什麼 |
|---|---|
| `contracts.py` | `ENCODER_VERSION` 2 → **4**。加 target head 的詞彙（§3）、市場詞彙與 mask（§4）、`legal_unit_mask` 補五條（§5） |
| `harness/build_dataset.py` | `segment_labels()` 反推目標格；市場標籤攤成逐盤面的稠密形式 |
| `model/net.py` | target head（100 選 1）+ market head（present 21 / qty 21×18） |
| `model/train.py` | 對應的 loss 與指標、`--labels` A/B、`--train-episodes`（畫 scaling 曲線）、`torch.manual_seed` |
| `serving/npz_forward.py` | 兩個新 head 的 numpy 前向 |
| `agents/gen2_capped.py` | gen2 + 建物上限的單變數對照組（§2） |
| `agents/gen3_target.py` | 走目標承諾；`model_market` 決定市場走網路還是 gen0 |
| `tools/action_dist.py` | **kill switch 1**：自己打一局，比對動作分布 |
| `tools/state_dist.py` | **kill switch 2**：按天比對狀態分布，看第幾天掉出老師的 p5 |
| `tests/test_target_labels.py`、`tests/test_market_labels.py` | 13 項新測試 |

L0 從 **58 passed 變成 71 passed**。

---

## 2. 先確認接線沒壞（四道，全過）

昨天的懷疑清單逐條驗掉：

| 驗什麼 | 結果 |
|---|---|
| observation / action 對齊 | mask 擋掉老師動作 **0.34%**（錯位時是 14.66%） |
| `step` 的 fallback | `step == day*24 + hour` 對 720 步全部成立 |
| 兩個玩家的 observation | p0 / p1 都帶完整 `private` 和 `player`，merge 正確 |
| serving 那一層吃掉多少 | teacher forcing 走**比賽端真正那條路**（`encode` → `NumpyPolicy` → `legal_unit_mask` → `_choose`），驗證集 2 局 27,658 個 unit-turn：raw argmax 0.9689 → 加 mask 0.9666 → 加種子仲裁 0.9666。**只吃掉 0.23 個百分點** |

> 🩸 同一支腳本跑**訓練集**裡的 2 局是 0.9976。差別就是記憶 vs 泛化 ——
> 量 teacher forcing 一定要用驗證集的那 6 局（`np.random.default_rng(0)` 切出來的
> 是 93955905 / 94041982 / 93990240 / 93835266 / 94173911 / 94079897）。

### 順手做的單變數對照：建物上限

`gen2_model` 期末建物 25.2 個（老師 14.0）。原因是 `BUILD_PASTURE` 是 unit 動作，
而 `config` 的 `n_structures` 只走 `gen0` 的市場那一路 —— gen2 把 gen0 的 unit 動作
丟掉了，所以那個參數**對建物數完全無效**。

加上限之後建物 25.2 → **14.0**（跟老師一樣），現金 8,002 → 9,156，仍然 0 勝 4 負。
**被擋掉的回合變成 PASS（8.1% → 11.7%），沒有變成生產動作。**

> **拿掉壞選項不會產生好選項。** 這條後面一再出現。

---

## 3. v3：把 unit 的「目標」變成顯式標籤

昨天的診斷是「老師的策略不是 observation 的函數 —— 這個 unit 要去哪不在任何欄位裡」。
今天把它變成標籤：一段連續 MOVE 加結尾那個非 MOVE 動作叫一個 **segment**，
segment 裡每一步的標籤是「終點在哪一格、到了做什麼」。

三個支持這個做法的實測（3 局 × 2 玩家、41,484 個 unit-turn）：

| | 數字 |
|---|---|
| MOVE 段落平均長度 | 1.79 步（56.4% 只有 1 步、99.4% ≤ 7 步） |
| **`gen0.step_toward` 重現老師實際那一步** | **93.02%**（18,372 / 19,750） |
| 走到一半就日終、沒有終點動作 | 4.2%（60 局全量是 3.6%），標成 -1 不算 loss |
| 段落終點動作 | WATER 34.2%、HARVEST 12.5%、PASS 12.5%、FEED 9.7%、COLLECT_FERTILIZER 9.6% |

第二列是關鍵：**路徑不用學。** `_step_toward` 改成公開的 `step_toward`。

### 訓練起來了，實戰仍然沒有

```
op（終點動作）  0.9204   dummy 0.3417（永遠猜 WATER）
target          0.8716   dummy 0.5081（猜自己現在這格）
value RMSE      0.1433   dummy 0.2157
```

target 0.87 對 dummy 0.51 —— **目標確實是盤面的函數**，這個假設成立。

```
                gen2_model   gen3_target   老師
MOVE               75.6%        36.9%     51.8%
PASS                8.1%        35.9%      9.1%
平均現金           8,002        2,405   140,210
```

**擺盪修好了（MOVE 甚至低於老師），但錢更少。** `action_dist` 的紅線從 5 條減成 3 條。

診斷（實測一局 6,353 個 unit-turn）：63.3% 的回合 unit **已經在目標上**，而到了目標
之後 op head 的第一名有 **46.1% 是 PASS**（訓練標籤裡 PASS 只佔 12.5%）。原因是
農場上沒有動物 —— 老師 24.1% 的終點動作是 FEED / CARE / COLLECT_FERTILIZER，
沒動物那三件事在物理上做不到，unit 就真的沒事幹。

**而動物是市場買的，不是 unit 決定的。** 所以往 §4。

---

## 4. v4：市場也交給網路

昨天到今天早上市場一直沿用 `gen0._market`。量下去才知道那是致命的：

| | 老師（2 局平均） | gen0 的市場 |
|---|---|---|
| 整季賣出 | $185,358（1,553 個） | $9,977（90 個） |
| BUY_PRODUCT WHEAT（飼料） | 553 個 / $22,921 | 13 個 / $390 |
| BUY_ANIMAL | COW 10 + SHEEP 4 | COW 3 |
| 工資與買地 | $8,977 | $6,296 |

**花了對方 70% 的人事成本，產出 5%。**

### 🩸 兩個抽取上的坑

1. **同一回合同一個 op 會重複** —— 佔 9.6% 的 (回合, op) 組合。數量必須**相加**
   不是取後者：引擎逐單位重新報價，`SELL 5` + `SELL 5` 與 `SELL 10` 完全等價；
   `HIRE` 兩筆就是雇兩個人。
2. **`HIRE` 一次要送 n 筆。** 實測老師 **57.6% 的雇工回合一次送 5 筆**（最多 9 筆）
   —— hands 每天被清空，開工那一刻要一次補滿。第一版的 `decode_market` 只送 1 筆，
   等於每天上午都在慢慢補人。`clamp_market_qty` 用 fib 價格表算「還雇得起幾個」。

### 🩸 mask 的第一版擋掉老師 23.1% 的訂單

把「錢夠不夠」「shed 有沒有貨」寫進 `legal_market_mask`，結果 3,484 筆裡擋掉 804 筆
（`SELL FERTILIZER` 406、`SELL WHEAT` 234）。

因為**老師自己就會送出送不成的訂單** —— journal 08-19 §5 早就量過：整季送出約
1,460 筆 `FERTILIZER SELL`，實際只成交約 449 個。那是「有多少賣多少」的寫法。

照不變式該**放寬 mask**，不是調高門檻。那些條件搬到 `clamp_market_qty`，
`legal_market_mask` 現在只留「象限買完了不能再買地」一條。

### 結果：更差

```
                gen3（市場走 gen0）   gen4（市場走網路）   老師
平均現金             4,559                2,061        ~140,000
買地                1.00 塊               0 塊          2.00 塊
MOVE                 33.5%                23.5%          51.8%
PASS                 49.7%                68.4%           9.1%
雜草率               16.7%                35.2%           2.7%
動物掉出 p5          day 1                day 1            —
現金掉出 p5          day 6                day 2            —
```

market head 在實戰的下單頻率只有老師的 **4%**：

```
                      我們/回合   老師/回合    比值
SELL WHEAT              0.0083     0.2171    0.04
SELL FERTILIZER         0.0042     0.1911    0.02
BUY_SEED WHEAT          0.0014     0.1120    0.01
BUY_LAND                0.0000     0.0030    0.00   ← 整局沒買過地
```

訓練時 market F1 0.7331（P 0.84 **R 0.65**）—— 在老師的局面上就漏掉 35%，
自己下場再掉 16 倍。**跟 §5 的 WATER 是同一個形狀。**

---

## 5. `legal_unit_mask` 漏了五條，老師一次都沒犯過

`gen3_target` 送出的 852 次 `WATER` 裡，**501 次（58.8%）澆在今天已經澆過的格子上**
—— 引擎靜默 no-op。有效澆水只剩 351 次/局，老師是 928 次。作物連續兩天沒澆就變雜草。

老師的白費率（3 局 × 2 玩家、13,466 個動作）：

```
WATER 0.00%（5,569 筆）   FEED 0.00%（1,777）   CARE 0.00%（1,867）
COLLECT_FERTILIZER 0.00%（1,868）   HARVEST 0.08%（2,385 筆裡 2 筆）
```

所以這五條**可以加而不違反 mask 的不變式**。它們是漏掉，不是刻意留給網路學的。
補上之後白費動作 **9.2% → 0.0%**。這是今天唯一乾淨的一勝。

### 網路學不學得會這五條？學得到八成，但不夠

| | 老師的局面上 | 自己下場 | 倍率 |
|---|---|---|---|
| 全部白費動作 | 1.02% | 9.2% | 9× |
| WATER | 4.08% | 58.8% | **14×** |

規則本身是 AND 條件的硬規則（`kind == PLANT` **且** `not watered_today`），
五個 channel 全都在輸入裡。mask 一行就是 0%，網路永遠會有殘差，而殘差在閉迴路
會被放大十幾倍。

---

## 6. 資料面查了三件事，全部排除

### 6.1 老師不是在跑固定腳本（但開局是）

60 局全部是**不同的 seed**，カワシギ 出現在全部 60 局。它自己跨局的 unit 動作
一致率中位 **39.3%**（66 組兩兩比對，隨機基準約 10%）。所以資料的多樣性是真的。

**但開局前 73 步（3.0 天）逐字相同**，最長有一局到 426 步。719 個 step 裡，
farmer 動作在 12 局中完全相同的有 226 個（31.4%）。

⚠️ 「カワシギ vs Thomas 的跨隊一致率 45.1% 比自己跨局的 39.3% 還高」這個數字
**是被污染的** —— 12 局裡很多局兩人同場，同一個 seed、同一個市場，本來就會比較像。
不要拿它下結論。

### 6.2 邊際分布很寬，要按天比才看得出來

老師 60 局的邊際：現金 p5 只有 $23、雜草 p95 是 13、作物 p1 是 0。單看邊際，
我們的局面都「在範圍內」。但那些低點全部發生在開局。按天條件之後：

```
 day |  動物 p5  中位  我們 |  作物 p5  中位  我們 |   現金中位     我們   百分位
   6 |       6     6     1 |      16    19    11 |       982      906    45.5%
  10 |      11    12     1 |      24    33    36 |     1,778      638    10.7%
  14 |      14    14     1 |      52    59    44 |    17,242    1,200     0.0%
```

**第一個掉出去的是動物（day 1），那時候現金還完全正常。** 這條先後順序就是
`tools/state_dist.py` 存在的理由。

### 6.3 data-scaling 曲線：還在升，但升不到要的地方

固定驗證集、只砍訓練集：

```
訓練局數   board 樣本    op       target    market F1   market recall
   6        8,628     0.6971    0.5576     0.5054        0.41
  13       18,694     0.8045    0.6963     0.5766        0.48
  27       38,826     0.8600    0.7925     0.6410        0.55
  54       77,652     0.9166    0.8680     0.7331        0.65
```

每倍增的增量：op +0.107 → +0.056 → +0.057（減速），target +0.139 → +0.096 → +0.076
（減速），**market F1 +0.071 → +0.064 → +0.092（沒有減速）**。

market head 是唯一連轉平的樣子都還沒出現的 —— 它最餓資料。但看 §7 的算術。

---

## 7. ⚠️ 定死這件事的數字：開局腳本只跟得完 22%

拿**跟老師同一個 seed**（1220298539 / episode 93783010）跑，比對前 73 步：

```
區間              farmer 對        全部 unit 對
day 0（1~24）    10/24 =  42%     66/139 =  47%
day 1（25~48）    2/24 =   8%      2/24  =   8%
day 2（49~73）    4/25 =  16%      4/117 =   3%
整段腳本         16/73 =  22%     72/280 =  26%
```

盤面：

```
step        我們 作物/建物/動物/草  人   現金   |   老師 作物/建物/動物/草  人   現金
  24         11    4    3   0     0    498    |    19    4    4   0    0      23
  72          0    4    1  11     0    385    |    19    5    4   0    0      49
```

**day 2 結束時我們 0 作物、11 雜草，老師 19 作物、0 雜草。** 而且我們現金比老師多
—— 錢沒花出去。

### market head 在開局是對的

`gen4_market` 的 **step 1 市場訂單跟老師完全相同**（同一個集合）：
`HIRE ×5, BUY_ANIMAL COW 2, BUY_ANIMAL SHEEP 2, BUY_SEED WHEAT 7,
BUY_SEED MELON 12, BUY_PRODUCT WHEAT 6`。

`gen3_target`（走 gen0 的市場）第 1 步就錯（只買 1 隻 COW、每種種子各 1）。
**開局壞掉的是 unit 那一半，不是市場。**

### 為什麼是 22% —— 這是算得出來的

target 準確率 0.868，每一步有 13.2% 的機率走錯：

```
第一次走錯的期望步數    1 / 0.132 = 7.6 步
實測第一次分歧           第 6 步            ← 吻合
連續 73 步都對的機率     0.868^73 = 0.003%
要有一半機會跟完 73 步   0.5^(1/73) = 0.991
```

**一步走錯就永遠回不去腳本上。** 之後的盤面老師沒走過，比對本身就沒有意義了。

### 所以「抓更多 log」不是解

從 0.868 爬到 0.991，照 §6.3 的曲線（每倍增 +0.076 且在減速）需要**四次以上的倍增**
（60 → 960 局），而且準確率一定會在某處平掉 —— 老師有些決定本來就不是盤面的函數。
就算推到 0.95，`0.95^73` 還是 2%。

**抓弱 player 的 log 也不是解。** 他們的局面確實長得像我們（雜草多、動物少），
但他們在爛局面裡的動作是「一路爛下去」，不是「怎麼救回來」。而且 08-19 §7d 已經
量過混不同老師會更差。真要用的話唯一正確的用法是 rating-conditioned
（把 rating 當輸入欄位、推論時填最高值），但那沒有解決乘法問題。

> **要脫離這個乘法，只有兩條路：**
> 1. **降低需要連對的步數** —— 每一步都能自己修正，不依賴前一步對
> 2. **放棄模仿改用 search** —— 自己評估這一步好不好，不需要跟腳本
>
> 第 2 條就是 `workflow.md` §1 圖上原訂的路線，而 value head 已經訓出來了
> （RMSE 0.1474 vs dummy 0.2157）。缺的是 forward model。

---

## 8. 兩個新的 kill switch

08-19 §7d 的教訓是「準確率不是有效的 kill switch」。今天做了兩個替代品，
都不用標籤、一局就跑得出來（4 workers 約 1,400~1,600 局/小時）。

```bash
python -m eval.runner --a <版本> --b ladder-top-a --games 2 --workers 4
python -m tools.action_dist temp/<run 目錄>     # 動作分布
python -m tools.state_dist  temp/<run 目錄>     # 狀態分布，按天
```

| | 紅線 | gen2 | gen3(v4) | gen4 |
|---|---|---|---|---|
| `action_dist` | MOVE 差 ≤ 10pp、生產動作 ≥ 對方 10% | 5 條紅 | 2 條 | 2 條 |
| `state_dist` | 動物/作物/現金撐到 day 14 才掉出 p5 | — | 動物 day 1 | 動物 day 1 |

⚠️ **`state_dist` 那條紅線從來沒有綠過。** 每一版的動物都是 day 1 就掉出去。

---

## 9. 現在的檔案狀態（都還沒 commit）

```
權重         model/weights.npz         v2  ← 不要覆寫，gen2 的 baseline 靠它
             model/weights-kawashigi.npz v2
             model/weights-v3.npz      v3  ← 已被 ENCODER_VERSION 4 作廢
             model/weights-v4.npz      v4  ← 現行
checkpoint   model/checkpoints/ (v2)、model/ckpt-kawashigi/ (v2)、
             model/ckpt-v3/、model/ckpt-v4/
資料         data/dataset/  60 局、86,280 board、842,151 unit、24 MB、v4
run 目錄     temp/20260820-100037_gen2_model_*      8,002
             temp/20260820-101655_gen2_capped_*     9,156
             temp/20260820-110349_gen3_target_*     2,405（v3 權重）
             temp/20260820-121801_gen4_market_*     2,061
             temp/20260820-121815_gen3_target_*     4,559（v4 權重、gen0 市場）
```

⚠️ `gen2_model` / `gen2_capped` 的權重是 v2，現在會被 `_policy()` 的版本檢查擋下來
（拋 `SystemExit`，不是無聲失敗）。要重跑它們得先把 `ENCODER_VERSION` 降回去，
或重訓 —— **baseline 數字已經記在上面，不需要重跑。**

⚠️ **榜上跑的仍然是規則式 Gen1。** `main.py` import 的是 `agents.gen1`，
`submission/` 裡只有 `gen0.py` / `gen1.py` / `main.py`，沒有 `contracts.py`、
`npz_forward.py`、任何 `.npz`。今天的網路一次都沒上過場。

---

## 10. 這一輪不做的事

- **再抓 log**（頂端的或弱的都是）—— §7 的算術說服不了我
- **調 lr / epochs / 加大網路** —— teacher forcing 已經 0.92，卡住的不是那裡
- **繼續在 gen0 的參數空間掃參數** —— 08-19 §6.4 的 16 個變體已經證明走不通

---

## 11. Phase 3：DAgger 三輪，跑到 kill point

§7 的結論是「要脫離乘法，只有降低連對步數或改用 search」。當天稍晚把 DAgger
蓋起來跑了三輪 —— **它修掉了時間軸上的乘法，但撞到 unit 軸上的另一個乘法。**

### 兩個定案的決定

> **1. 提交端就是純網路，不做推論時搜尋。**
> 搜尋只在離線訓練時跑。推論時搜尋要呼叫引擎私有函式，而 `main.py` 底部已經
> 記過 1.29.3 與 1.32.7 的 `_commit_unit` 簽章不同、多傳一個參數的結果是
> **每一回合都 `TypeError`**，ladder 用哪個版本看不到。離線搜尋沒有這個風險，
> 也沒有 `actTimeout: 1` 秒的限制。
>
> **2. expert 做成可換的**，第一輪插 gen1 驗迴圈，之後換離線 search。

### 為什麼 gen1 可以當 expert

**DAgger 需要一個問得到答案的 expert。** 老師是 60 份固定 replay，問不到新局面
—— 這就是之前用不了 DAgger 的原因。gen1 問得到（每回合從當下盤面重算），而且
2026-08-20 量到它對老師是 **0 條紅線**（每條產線都在老師的 51~97%）。

⚠️ 代價是**天花板就是 gen1**（$73,150）。第一輪的價值在別處：驗證閉迴路修得掉，
以及產出 self-play 資料。

### 蓋的東西

| 檔案 | 做什麼 |
|---|---|
| `agents/gen0.task_destination()` | 從 `_task_action` 拆出來的「終點格 + 到了做什麼」。兩邊共用一份判斷，`_task_action` 只剩四行 |
| `agents/gen0.act(return_plan=True)` | 交出每個 unit 的 `(終點格, 終點動作)`。**那正好是 v3 的標籤格式** —— 我們自己的 agent 沒有理由像 replay 那樣從 MOVE 段落反推 |
| `harness/rollout.py` | `--policy` 開車、`--expert` 貼標籤，一局一個 npz，欄位跟 `build_dataset` 逐項相同 |
| `harness/build_dataset.dense_market()` | 市場稠密化抽出來共用 |
| `model/train.py --data a,b,c` | DAgger 的 Aggregation |
| `config/opponents/dagger.json` | ⚠️ **不可以加 params 覆寫** —— 網路學的是 gen1 預設參數下的行為 |

吞吐：資料收集 9,150 局/小時（policy=expert）、3,538 局/小時（policy≠expert，
兩個都要算）。**磁碟遠比預估小** —— 我原本估 200 局 2.2 GB，那是未壓縮的算法，
實際壓縮後只有 20~34 MB。

### 三輪的曲線

```
              round 0   round 1   round 2      gen1      老師
平均現金            0    14,745     8,886    73,150   142,689
action_dist 紅線  4 條      1 條      1 條      0 條         —
PASS            59.2%     33.8%     26.9%     18.1%      9.1%
生產動作         22.1%     26.3%     32.5%     25.9%     38.9%
MOVE            18.6%     39.8%     40.5%     55.8%     51.8%
雜草率          29.4%      5.7%      7.0%      1.6%      2.7%
現金掉出 p5         —    day 14    day 13         —         —
target 準確率   0.7580    0.6505    0.6301         —         —
```

⚠️ **round 0 不是 DAgger 是 BC**（policy 和 expert 都是 gen1），所以拿 $0 是
預期內的。DAgger 的修正從 round 1 才開始。計畫裡把 round 0 的門檻寫成 $55k
是寫錯了。

### 🩸 訓練準確率下降、實戰大幅變好

round 0 → round 1：op 0.9170 → 0.8681、target 0.7580 → 0.6505，現金 $0 → $14,745。

資料集變難了（多了網路自己走出來的爛局面），所以準確率掉。**這是 DAgger 的
典型現象，也是「準確率不是 kill switch」的第三個獨立證據**（前兩個是 08-19 §7d
和今天的 §7）。

### ⚠️ 乘法從時間軸搬到了 unit 軸

動作分布一直往 gen1 靠 —— 生產動作 32.5% 已經**超過** gen1 的 25.9% —— 但現金
只有它的 1/8，而且 round 2 還掉回去。**做的動作種類對了，做在錯的格子上。**

target 準確率停在 0.63，而每回合要同時把 12 個 unit 放對位置：

```
0.63 ^ 12 = 0.4%          （對照 §7 的 0.868 ^ 73 = 0.003%）
```

同一個結構，只是從「連續 73 步都要對」變成「同一回合 12 個 unit 都要對」。

而且 target 有一部分**本質上學不起來**：`_minimum_cost_assignment` 是全域最小
成本配對，兩個 unit 離同一格一樣近時派誰去都對，而逐 unit 計較身分的 loss 在
那些情況下是在擬合雜訊。target 準確率隨著資料變多**反而下降**
（0.7580 → 0.6505 → 0.6301），符合這個解釋。

### 下一步（未動手）：網路預測「需求」，配對還給演算法

gen1 決定 unit 去哪是兩步：

```
步驟 1  掃盤面 -> 「(3,5) 要澆水、(7,2) 要收成」   ← 判斷，是盤面的函數，有唯一答案
步驟 2  把 12 個人配到這些工作，總距離最短        ← 配對，最佳化演算法，算得出最佳解
```

**現在網路把兩步一起做掉了。** 提議是只做步驟 1，步驟 2 還給
`_minimum_cost_assignment`（gen1 實測它對貪婪分派器 58 勝 2 負）。組合爆炸那一段
就完全不經過網路。

這跟 journal 08-19 §7e 的 option B 同方向，但當時沒有 target 準確率這個量測，
說不出「為什麼」。

### self-play 資料是這一輪唯一可以帶走的東西

value head 在 round 0 的資料上 RMSE **0.0803 對 dummy 0.2515**（3.1 倍），
而 replay 資料只有 0.1474 對 0.2157（1.5 倍）。而且組內變異數佔總變異數 48.6%
（同一個對手內部標準差 $14,925）—— 那份資料裡確實有「打得好不好」的訊號，
不只是「對手是誰」。**離線 search 需要的評估函式有著落了。**

---

## 12. schema v5：網路只預測需求，配對還給演算法 —— 過了

§11 提的那件事做完了。**結論：kill point 解除，網路重現 gen1 的 92.3%。**

### 切法

| | 誰做 | 為什麼 |
|---|---|---|
| 哪一格該澆水、值不值得施肥 | **網路**（demand head） | 是盤面的函式，有唯一答案 |
| 誰去哪一格 | `_minimum_cost_assignment` | 算得出最佳解，不會錯 |
| 走哪一步 | `gen0.step_toward` | 對 60 局 replay 命中 93.02% |
| 要幾趟 `FETCH_*` | `gen0._tasks()` 的補給段 | 是 FEED / FERTILIZE / PLACE 格數的導出量 |
| `PLANT` 種什麼、`PLACE` 放哪種 | 查表（`crop_for` / `_house_animals`） | 不是判斷 |

`contracts.TASK_OPS` 是 11 個 op，demand map 是 `[11, 100]` 的 multi-label。

### 準確率：0.6301（v4 逐 unit）→ 0.9804（v5 逐格）

```
              v4 target 準確率          v5 demand F1
round 0            0.7580                 0.9804（dummy 0.3677）
round 1            0.6505                  ——
round 2            0.6301                  ——
```

v5 的 dummy 是「**所有合法的格子都做**」（召回率 1，精確率 = 需求佔合法格的
比例）。實測每個盤面 37.5 筆需求 / 167.2 個合法格 = 22.4%，F1 0.3677。
不是「永遠說沒有」（那個 F1 是 0，贏過它不代表學到判斷）。

v4 的準確率是**資料愈多愈低**，v5 沒有這個問題 —— 一格該不該澆水跟哪個 unit
去做無關，`_minimum_cost_assignment` 的任意 tie-break 不再進到標籤裡。

### 實測（40 局配對種子，gen4_rulemarket vs gen1）

```
                gen4_rulemarket      gen1
平均現金             85,711         92,860     （92.3%）
勝/和/負            2 / 0 / 38
MOVE                  56.8%          56.4%
生產動作              25.7%          26.2%
PASS                  17.4%          17.3%
全季作物              49.2%          45.7%
期末作物            18.7/75.0      16.2/75.0
雜草率                 2.7%           1.9%
買地                2.00 塊        2.00 塊     （第 1 塊都是 day 9.0 NE）
action_dist 紅線      0 條           0 條      （對 ladder-top-a）
```

⚠️ **範圍**：這是 `model_market: false`（市場走 `gen0._market`）的數字。
§11 那三輪是對 ladder-top-a 各 2 局量的，seed 數不同，兩張表的「平均現金」
不能直接相減。可比的是「相對 gen1 的比例」：round 2 是 12%，v5 是 92%。

計畫裡 Stage 1 的判定門檻是 gen1 的 75%。**過了。**

### 市場那一半仍然是主要缺口

同一份權重、同樣 4 個 seed 對 ladder-top-a，唯一差別是 `model_market`：

```
                     model_market: false    model_market: true
平均現金                    69,140                27,965
PASS                        17.7%                 47.5%
生產動作                    25.2%                 13.5%
期末作物                  14.2/75.0              4.1/56.2
買地                       2.00 塊               1.25 塊
```

差 $41,175，佔全部差距的 60%。market head 的 F1 是 0.8105，但**召回率只有
0.72** —— 少下的那 28% 訂單裡有種子和雇工，所以田是空的、人是少的，
unit 沒事幹就 PASS。這跟 v3→v4 記到的「只模仿一半的 policy」是同一個病，
只是這次剩下的那一半是市場。

**下一個要處理的就是它**，而且它跟 demand 是獨立的兩個 head，可以單獨動。

### 改了什麼

```
contracts.py         v4 -> v5：TASK_OPS（11 個）、N_TASK_OPS、TASK_OP_INDEX、
                     legal_demand_mask()
agents/gen0.py       _tasks() 拆成 _tile_tasks()（規則）+ demand_tile_tasks()（網路），
                     補給段兩邊共用；act(demand=...)；plan 多回傳 tasks
agents/gen1.py       demand 照傳
agents/gen4_demand.py  新：網路出 demand map -> gen0 配對走路
model/net.py         demand_out（1×1 conv）+ demand_logits()，forward 回 7 個
serving/npz_forward.py  對應的 numpy 版本
model/train.py       demand 的 BCE（只在合法格上算）+ F1 指標 + --demand-weight
harness/rollout.py   錄 board_demand_bits / board_demand_legal_bits（packbits），
                     每回合驗「需求 ⊆ legal_demand_mask」
config/opponents/    gen4_demand.json、gen4_rulemarket.json
tests/               test_demand_labels.py（5 項）、test_contracts.py 多一項
```

**gen0 沒有 import numpy / contracts 到模組層**（`demand_tile_tasks` 裡是
函式內 import，submission 路徑走不到），submission 的相依性沒變。
L0 **77 passed**，`test_l0_smoke` 的固定 seed 期末現金逐項相同 —— 規則式那條路
一個 bit 都沒動。

### 這一輪最有用的一項測試

`tests/test_demand_labels.py::test_demand_map_round_trips_to_the_same_action`：
跑一局 gen1，把它自己的 demand map 餵回 `_tasks(demand=...)`，**送出的動作
（farmer / hands / market）必須逐項相同**，連續 200 個回合。

它成立就代表切口沒有失真 —— gen4 跟 gen1 之間只剩「網路預測得準不準」這一個
變數。切口漏東西的三種寫法（少一種 op、x/y 顛倒、`arg` 沒補上）都不會報錯，
只會表現成分數莫名其妙變低。

---

## 附：怎麼重跑今天的數字

```bash
# §12 的 v5（現況）：rollout -> 訓練 -> 匯出 -> 配對對戰 -> kill switch
python -m harness.rollout --policy gen1 --expert gen1 \
       --games 200 --workers 26 --out data/dagger/v5-round0
python -m model.train --data data/dagger/v5-round0 --out model/ckpt-v5-round0 \
       --epochs 8 --width 96 --blocks 6
python -m serving.export_npz --ckpt model/ckpt-v5-round0/best.pt \
       --out model/weights-v5-round0.npz
KAGGRI_WEIGHTS=model/weights-v5-round0.npz python -m eval.runner \
       --a gen4_rulemarket --b gen1 --games 20 --workers 20
KAGGRI_WEIGHTS=model/weights-v5-round0.npz python -m eval.runner \
       --a gen4_rulemarket --b ladder-top-a --games 4 --workers 4 --log-level 2
python -m tools.action_dist temp/<run 目錄>

# 市場那一半值多少：同一份權重，唯一差別是 model_market
KAGGRI_WEIGHTS=model/weights-v5-round0.npz python -m eval.runner \
       --a gen4_demand --b ladder-top-a --games 4 --workers 4 --log-level 2

# 全套：抽資料 -> 訓練 -> 匯出 -> 對戰 -> 兩個 kill switch
# ⚠️ 老師的 replay 產不出 v5 的 demand 標籤（沒有任務清單可讀），
#    所以 data/dataset 現在只餵得動 v4 以前的訓練。
python -m harness.build_dataset temp/episodes --out data/dataset
python -m model.train --data data/dataset --out model/ckpt-v4 \
       --epochs 8 --width 96 --blocks 6
python -m serving.export_npz --ckpt model/ckpt-v4/best.pt --out model/weights-v4.npz
KAGGRI_WEIGHTS=model/weights-v4.npz python -m eval.runner \
       --a gen4_market --b ladder-top-a --games 2 --workers 4
python -m tools.action_dist temp/<run 目錄>
python -m tools.state_dist  temp/<run 目錄>

# §6.3 的 data-scaling 曲線（驗證集不動，只砍訓練集）
for n in 6 13 27 54; do
  python -m model.train --data data/dataset --out /tmp/ckpt-$n \
         --epochs 8 --width 96 --blocks 6 --train-episodes $n
done

# §5 的白費動作、§7 的開局比對：兩支臨時腳本沒有進版控，
# 做法寫在上面，重寫大約各 40 行。要點：
#   - 白費：包住 gen3_target._choose_actions，檢查送出的動作在當下的 tile 上
#           是不是 no-op（watered_today / fed_today / cared_today /
#           fertilizer_available / yield_units）
#   - 開局：用 episode 93783010 的 seed 1220298539 跑自己的 agent，
#           逐步比對 steps[t][0].action 與該局カワシギ的 action
```

⚠️ 比對 teacher forcing 一定要用**驗證集**那 6 局（§2 的 🩸）。
