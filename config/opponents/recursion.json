{
  "name": "recursion",
  "entry": "agents.replay:act",
  "params": {
    "episode": "93848465",
    "player": 1
  },
  "note": "rating 3019.2。8 支裡唯一種瓜的（12 建物、6 格 MELON、14 個人）。 從 config/ladder-top.json 抽出來成獨立 spec，讓 eval.runner.load_spec 叫得到（它只找 config/opponents/<name>.json）。⚠️ 開迴路 replay，細節見 agents/replay.py docstring。"
}
