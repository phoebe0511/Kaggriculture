{
  "name": "melon",
  "note": "西瓜的配額倍數。全域 oversupply_factor 是 1.5，這裡只覆寫 MELON。MELON 日需求固定 1.0（沒有任何 shop 要它），但 base $250 全場最高。A = ref-v1 固定。 ⚠️ 2026-08-21 更新：(1) `agents/gen1.py` 併入 `agents/gen0.py`，`gen0.DEFAULT_PARAMS` 多了 gen1 的 21 項（n_structures 9->12、optimal_assignment False->True、tiles_per_unit、water_on_demand 等）。**這個 sweep 內部仍然可比**（所有變體共用同一組新預設），但**不能拿現在的數字跟 2026-08-16~19 那批比**。 (2) Gen0 線的 9 個對手已刪除（ref-v1 / ref-v2 / gen0 / 5 個 gen0-* / pq4）。固定 A 方請改用 `ref-v11`（唯一把 51 項參數完整展開的，不會隨預設值飄）。",
  "opponents": [
    {
      "name": "melon-1.5",
      "entry": "agents.gen0:act",
      "params": {
        "crop_oversupply": {}
      }
    },
    {
      "name": "melon-3",
      "entry": "agents.gen0:act",
      "params": {
        "crop_oversupply": {
          "MELON": 3.0
        }
      }
    },
    {
      "name": "melon-4",
      "entry": "agents.gen0:act",
      "params": {
        "crop_oversupply": {
          "MELON": 4.0
        }
      }
    },
    {
      "name": "melon-6",
      "entry": "agents.gen0:act",
      "params": {
        "crop_oversupply": {
          "MELON": 6.0
        }
      }
    },
    {
      "name": "melon-10",
      "entry": "agents.gen0:act",
      "params": {
        "crop_oversupply": {
          "MELON": 10.0
        }
      }
    }
  ]
}
