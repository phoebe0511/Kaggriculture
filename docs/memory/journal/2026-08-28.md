# 2026-08-28：CMA-ES 兩輪 —— 有效但幅度不夠，而且量到真正的差距是 −46,912

⚠️ **接手的人先看 §4。** 那裡有一個之前沒量過的數字：`gen0` 預設參數對 8 支真實
ladder 頂端玩家，**640 局只贏 19 局（3.0%）、平均差 −46,912**。CMA-ES 兩輪找到的
改進幅度（對 `gen1` 贏 14,302）就算完全轉移，也只把現金比從 57.9% 推到約 70% ——
**還是輸掉九成的局**。

- **第一輪失敗在 `--seeds 24`**（§1）：第 30 → 40 代 train +4,197 但 holdout
  −12,894、`gen1` −10,440，五欄全退。08-25 量到 margin 要 **54 個 seed**。
- **第二輪（seeds 54、holdout 3 支 → 9 支）有效**（§2）：第 10 → 100 代 9 支裡
  8 支變好，`gen1`（唯一會反應的）+3,242，所以「專門吃 ladder-top-a」和
  「吃 replay 不會反應」兩個擔心都沒發生。但第 68 代之後 39 代沒動。
- 🩸 **`best.json` 是用 train 分數挑的，挑錯了**（§3）：第 50 代的 holdout 好
  1,817、`gen1` 好 4,775，九隊平均只差 135（雜訊）。
- **驗收（§5）**：對**會反應**的對手 `gen1`，`cma1-g50` 我方現金
  **+12,716**、勝率 **95.0%**，而且雙方合計變大 —— 改進是真的。
  但對 8 支開迴路 replay 的「現金比 57.9% → 76.7%」**有 81% 來自壓低對手**，
  那一段不會轉移到真人身上。
- **打包好了（§5b）**：`submission/cma1-g50/`，640 局跟原始碼版逐位元組相同。
  🩸 開子資料夾 —— `build_submission` 會 `rmtree` 整個 `dest`，而 `submission/`
  底下有 `e2e-round6/`。

---

## 1. 第一輪：`--seeds 24` 太少，第 30 代之後在背 seed

```
temp/cma/20260827-130938
--seeds 24 --popsize 14 --workers 10 --objective margin --opponent ladder-top-a
一代 336 局 / 228 秒   跑到第 54 代放 STOP
```

`best_so_far`：−51,786（第 1 代）→ −28,341（14）→ −19,658（23）→ −15,461（38），
之後 16 代沒動。

```
     代     train   holdout        差      tetsuya  recursion    utkarsh       gen1
    10   -35,639   -44,711    9,072      -47,411    -50,535    -58,831     -8,218
    20   -28,341   -27,820     -521      -13,736    -39,581    -44,706     +4,926
    30   -19,658   -23,179    3,520       -7,570    -24,088    -30,980     +8,910
    40   -15,461   -36,072   20,611      -13,038    -38,398    -53,259     -1,530
```

**第 30 → 40 代：train +4,197，但其它五欄全退**（holdout −12,894、utkarsh
−22,279、recursion −14,310、gen1 −10,440、tetsuya −5,468），`train − holdout`
從 3,520 爆到 20,611。

根因：`--seeds 24` 低於實測需求。08-25 量到 margin 要 **54 個 seed** 才排得動
相鄰候選（`tools/param_search.py` 的 `OBJECTIVES` 註解），而 sigma 掉到 0.06
之後候選之間的差距就在那個量級。開跑前就標了這個風險，實際發生。

## 2. 第二輪：seeds 54、holdout 加滿 8 支

```
temp/cma/20260827-164222
--seeds 54 --popsize 14 --workers 10 --sigma0 0.08
--warm-start temp/cma/20260827-130938/gen_0030.pkl
一代 756 局 / 539 秒   跑到第 108 代放 STOP
```

`best_so_far`：−28,324（第 1 代）→ −12,875（6）→ −8,654（68），之後 **39 代沒動**
（sigma 0.0128）。

🩸 **第一輪和第二輪的分數不可比** —— 24 個 seed 和 54 個 seed 量的是不同的東西。
第二輪第 1 代的 −28,324 大致就是「第一輪第 30 代那組參數改用 54 個 seed 重量」。

### 兩個擔心都沒發生

```
變化（正 = 變好）        第10->第100   第60->第100
  train                   +4,222      +1,151
  holdout                 +1,225        -509
  kawashigi                 +964      +1,975
  thomas                  +6,751      +2,623
  tetsuya                 -3,754      -3,240
  utkarsh                 +5,836      +2,846
  peikopon                +5,539      +4,411
  lucien                  +7,035      +1,347
  recursion               +8,714      -1,472
  kostiantyn              +4,964      -2,050
  gen1                    +3,242      -7,454
```

第 10 → 100 代 **9 支裡 8 支變好**（只有 tetsuya 退），而且 `gen1`（唯一會反應的）
同向。所以：

- ❌ **不是**「專門吃 ladder-top-a」—— 沒訓練過的 7 支也跟著升
- ❌ **不是**「吃 replay 不會反應」—— `gen1` 會反應，也升

**訓練只打一支對手（`ladder-top-a`）這件事，事後看是可以接受的。** 但那是
holdout 給的事後判斷，不是設計上的保證。

第 60 → 100 代是白跑的：train 只 +1,151，holdout 和 5 支往回走。

### 這一輪改的三件事

| | 為什麼 |
|---|---|
| `--seeds` 24 → 54 | 第一輪的過擬合根因，實測需求 |
| `HOLDOUT_TEAMS` 3 支 → 8 支 + `gen1` | 原本只有 tetsuya/recursion/utkarsh，**看不到對 rating 最高的 kawashigi / thomas 有沒有效**。成本每次 checkpoint 多 100 局 ≈ 55 秒（+2.4%） |
| 新增 `--warm-start` | 借上一輪某一代的最佳解當起點，**covariance 重來、sigma 重設**。舊 covariance 是在雜訊排序下學的，而且正是過擬合的方向 |

## 3. 🩸 `best.json` 是用 train 分數挑的，挑錯了

```
              holdout      gen1    九隊平均
第 50 代      -18,712   +19,077     -17,412
第 68 代      -20,529   +14,302     -17,277   <- best.json 是這一組
```

九隊平均只差 135（雜訊），但第 50 代的 holdout 好 1,817、`gen1` 好 4,775。

**`best.json` 不能直接拿去用。** 兩組參數有 23/51 項不同。落地成候選：

```
temp/cma1-g50.json    第 50 代（從 gen_0050.pkl 的 es.best.x 取）
temp/cma1-g68.json    第 68 代（= best.json）
```

兩個都帶 `frozen: 2026-08-28` —— 那個 key 有作用不是註記，`eval/runner.py:135-137`
看到它會補 `_replace_defaults: True`，讓這 51 項完全不跟今天的 `DEFAULT_PARAMS`
合併。

## 4. 🔴 驗收：先量到的基準比預期糟很多

```
python -m eval.runner --a gen1 --ladder config/ladder-top.json \
    --games 40 --seed0 2000 --workers 10
產物 temp/20260828-092121_gen1_vs_ladder-top
```

seed 2000~2039，跟 CMA 的 train（0~53）和 holdout（1000~1039）完全不重疊。

```
gen1（= gen0 預設參數）對 8 支 ladder，640 局
  我方平均 64,432    對手平均 111,344
  現金比 57.9%       勝場 19/640 = 3.0%    平均差 -46,912

對手          勝/和/負   得分率   我方現金   對手現金        差
kawashigi      1/0/79     1.2%     61,842    109,879    -48,037
thomas         3/0/77     3.8%     62,214    108,517    -46,304
tetsuya       12/0/68    15.0%     73,119    103,268    -30,149   <- 唯一有勝率的
utkarsh        0/0/80     0.0%     62,707    116,509    -53,802
peikopon       0/0/80     0.0%     63,332    113,747    -50,415
lucien         2/0/78     2.5%     63,348    106,967    -43,619
recursion      0/0/80     0.0%     67,335    122,772    -55,437
kostiantyn     1/0/79     1.2%     61,562    109,097    -47,535
```

跟 08-24 記的「`gen1` 對 `ladder-top-a` 56.8%」對得上（64,030 vs 112,719）。

### 這個數字改變了排序

CMA-ES 對 `gen1` 贏 14,302（margin）。就算完全轉移到期末現金，也只是
−46,912 → 約 −32,600、現金比 57.9% → 約 70%，**仍然輸掉九成以上的局**。

**CMA-ES 是「把現有架構調到最好」，不是「換一個更強的架構」。** 要上榜前 10
需要的是後者。

（`cma1-g50` / `cma1-g68` 自己的驗收見 §5 —— 結論比這一節樂觀一些。）

## 5. 驗收：改進是真的，但對 replay 的數字灌水八成

seed 2000~2039 ×2（正反手配對），跟 CMA 的 train（0~53）和 holdout
（1000~1039）完全不重疊。

### 對 8 支 ladder replay（**不會反應**）

```
              我方       對手     現金比    勝率     雙方合計
gen1        64,432    111,344     57.9%     3.0%    175,777
cma1-g50    69,247     90,235     76.7%    14.4%    159,482
cma1-g68    69,419     90,510     76.7%    15.2%    159,928

相對 gen1：我方 +4,815    對手 -21,109    合計 -16,295
```

🩸 **現金比的改善有 81% 來自壓低對手，而且雙方合計少了 16,295。**
08-27 §3 量過這個遊戲**不是零和的**（錢從 market 賣出來），所以合計變小代表
在把餅做小。而那 8 支全是開迴路 replay ——
`tools/param_search.py` 的 `OBJECTIVES` 早就標了這個誘因問題。

### 對 `gen1`（**會反應**，= `agents.gen0:act` 走 `DEFAULT_PARAMS`）

```
                    我方      對手     現金比    勝率     雙方合計
gen1 vs gen1      81,641    81,641    100.0%    46.2%    163,282
cma1-g50 vs gen1  94,357    75,750    124.6%    95.0%    170,108   我方 +12,716
cma1-g68 vs gen1  89,671    75,565    118.7%    92.5%    165,236   我方  +8,030

cma1-g50 相對自打：我方 +12,716   對手 -5,891   合計 +6,825
-> 只有 32% 來自壓低對手，而且**餅變大了**
```

### 判讀

| | 壓低對手佔比 | 雙方合計 |
|---|---|---|
| 會反應的對手 | 32% | **+6,825**（變大） |
| 開迴路 replay | 81% | −16,295（變小） |

**兩件事同時成立：**

1. **改進是真的。** 對會反應的對手 +12,716、勝率 95.0%，而且餅變大 ——
   那是多產出來的，不是搶來的。
2. **對 replay 的 57.9% → 76.7% 被灌水了。** 八成來自 replay 買不到東西、
   雇不到人。真人會改買別的，那一段不會轉移。

真實的期望改善介於 **+4,815**（對 replay 我方自己的增量）和 **+12,716**
（對會反應的對手）之間；`3.0% → 14.4%` 的勝率提升應該是高估。

**`cma1-g50` 兩個測試都贏 `cma1-g68`**（+12,716 vs +8,030），加上 CMA 期間
holdout 也比較好 —— 出貨用 g50。

⚠️ `gen1` 自打的勝率是 **46.2% 不是 50%**。同一支 agent 兩邊對打、正反手都跑，
理論上該剛好一半，差 3 局（37/80）。可能是引擎 per-unit lockstep 的先後順序
（`kaggriculture.py:545`）造成的位置偏差，或有平手。**沒查** —— 但它讓上面
所有勝率有約 4pp 的系統性偏移。

## 5b. 打包：`submission/cma1-g50/`

🩸 **`serving/build_submission.py:157` 會 `rmtree` 整個 `dest`**，而
`submission/` 底下有 `e2e-round6/`（打包好的網路版，含 6 MB 權重）。所以
**開子資料夾**，用 `copy_files(dest=REPO_ROOT/"submission"/"cma1-g50")`
從 Python 呼叫（CLI 沒有 `--dest`）。`submission/main.py`、`gen0.py`、
`e2e-round6/` 都沒有被動到。

參數是**內嵌**在打包出來的 `main.py`（`PARAMS = {...}`），不是放一份 JSON
進去。那不是偏好，是硬限制：Kaggle 用
`exec(compile(raw, path, "exec"), {})` 載入，**exec 環境裡沒有 `__file__`**
（`build_submission` 的 docstring），`main.py` 無從知道自己在哪個目錄，
旁邊放 JSON 也開不起來。

🩸 **`PARAMS` 裡一定要有 `_replace_defaults: True`。** 驗收走的是
`eval.runner.build_agent`，它看到 spec 的 `frozen` 會自動補這一項
（`eval/runner.py:135-137`）；submission 不經過那條路。不補的話這 51 項會跟
`gen0.DEFAULT_PARAMS` 合併 —— 今天是 no-op，但之後 gen0 多一個預設 key，
上場的就跟量過的不是同一個 agent。

### 驗收打包有沒有出錯

`config/opponents/cma1-g50-sub.json` 走 **`builtin`** 分支 —— `build_agent`
直接把路徑交給 `env.run`，用 Kaggle 自己的 `exec` + `sys.path.append(exec_dir)`
載入，跟正式上場同一條路。

🩸 `--a submission/cma1-g50/main.py` 那種寫法**會炸** —— `load_spec` 走
`__import__`，找不到同目錄的 `gen0`。一定要用 `builtin` 的 spec。

因為引擎是決定性的，判準可以下得很硬：**打包版對 ladder 的 640 局必須跟
`config/params/cma1-g50.json` 逐位元組相同。**

```
python -m eval.runner --a cma1-g50-sub --ladder config/ladder-top.json     --games 40 --seed0 2000 --workers 10 --log-level 0

局數        原始碼版 640    打包版 640
逐局比對    640/640 完全相同
錯誤 / 沒跑完的局    0
```

另外用 `tests/test_l0_smoke.py::test_submission_entry_survives_a_whole_game`
的同一套方式跑過整局：

```
cma1-g50  seed 41001 vs starter   720 步  DONE/DONE  期末 143,898  2.4s
cma1-g50  seed  2000 vs starter   720 步  DONE/DONE  期末 130,855  2.4s
榜上那版  seed 41001 vs starter                      期末 135,366
```

### 產物在哪

```
config/params/cma1-g50.json       51 項參數完整展開（要用參數讀這個）
config/params/cma1-g68.json       對照組
config/opponents/cma1-g50-sub.json  驗打包用的 builtin spec
model/cma1-g50.pkl                CMA 狀態 320 KB（--warm-start / 重現用）
model/cma1-g68.pkl                308 KB
submission/cma1-g50/              打包好的：main.py（內嵌 PARAMS）+ gen0.py
```

⚠️ 本機 ladder 是 1.32.7 + 本機預設 config；Kaggle notebook 量到是 1.29.3 且
config 不同，**兩邊分數不能互相換算**。

## 6. 下一步：先量差距在哪，不要繼續盲調

**我們從來沒量過那 46,912 是怎麼輸掉的。** 少雇人？作物組合差？賣的時機差？
買地太晚？在不知道的情況下繼續調 34 個參數，是在可能錯的維度上優化。

`tools/revenue.py` 直接回答 —— 它 hook `_commit_unit`（`kaggriculture.py:652`，
每一單位成交的唯一入口），把**雙方**每一筆實際成交價攤開：

```
python -m tools.revenue --a gen1 --b recursion --seeds 0-9 --side both
```

`recursion` 是差最大的那支（−55,437）。兩分鐘。

### 關於 self-play（討論，未實作）

🩸 **08-27 的負面結果不能拿來說 self-play 不行。** 那是兩件不同的事：

```
search 失敗的地方   「在一個盤面上 51 個動作哪個最好」
                    要在 ±4,572 的擺盪裡分辨幾百元 -> 需要 656 次延續
RL 需要的           「幾百萬步平均下來梯度方向對不對」
                    雜訊會被樣本數平均掉
```

真正卡住的是**模擬速度**：

```
引擎純 Python，一局 720 步
規則式   ~1.5 局/秒（10 workers）= 1,080 步/秒
帶網路   gen2_model.act 約 11 ms/步 -> 約 0.3 局/秒 = 216 步/秒
10^8 步 / 216 = 129 小時 ≈ 5.4 天（一次嘗試，不保證成功）
```

`serving/` 的 inference server 架構就是為此準備的（`docs/CLAUDE.md`：self-play
worker 不 import torch、走 queue batch）。但 `docs/CLAUDE.md` 明確寫
「不要建議 PPO from scratch」，而且一次下注 5 天。**先做 §6 的量測再決定。**

### 已經確認不用做的

`gen0` **已經在用鎮上的店了**（`agents/gen0.py:399-521`）：`dynamic_basket` 依當局
開出來的 shop 決定種什麼，還會用「抽取有放回、上限 8 間」推未來需求。編碼也給了
網路（`contracts.py:541-542` 的 `shop_*` / `n_shops`）。

## 8. §6 的量測做了：38,085 的差距拆得開，但**修不掉**

`tools/revenue.py --a config/params/cma1-g50.json --b recursion --seeds 2000-2009
--side both`（10 局平均，帳完全對得平）：

```
                gen1      cma1-g50    recursion
賣出          101,436     116,517      157,224
採購           18,609      20,660       20,881
工資與買地     10,030       7,748       10,148
期末現金       75,798      91,110      129,195
落後          −59,508     −38,085         —
```

逐項拆（−41,831 + 1,346 + 2,400 = −38,085，完全對平）：

```
              cma1-g50    recursion       差
WHEAT          −9,397      +12,042    −21,439   ← 56% 的差距
STRAWBERRY     35,166       49,612    −14,446
MELON          11,921       19,995     −8,074
WOOL            9,213       14,277     −5,064
MILK           43,208       40,312     +2,896
CARROT          2,901            0     +2,901
TOMATO          1,713            0     +1,713
```

**不是賣價問題，是產量問題**：出貨總量 900 vs 1,399 個，均價我們幾乎每項都不輸
（STRAWBERRY 181.7 vs 175.4，還比較高）。

## 9. 🔴 逐日地使用率：我們常態空著 29 格，對手踩在 100%

使用者在 ladder replay 上看出來的，量出來確認了（每天 hour 12 取樣，10 局平均）：

```
 day |          cma1-g50          |          recursion
     |   地  作物  動物   空  用率 |   地  作物  動物   空  用率
   9 |   75  22.3  10.6   42  44% |   50  37.3  11.7    1  98%
  12 |   75  29.9  12.0   33  56% |   75  63.3  11.7    0 100%
  20 |   75  34.9  12.0   28  63% |   75  63.3  11.7    0 100%
  28 |   75  27.1  12.0   36  52% |   75  39.3  12.7   23  69%
  29 |   75  25.0  11.3   39  48% |   75  14.3  12.7   48  36%
```

- **地不是問題**：兩邊都 75 格，買地時機差 1 天（我們 day 7/9，對手 day 6/10，
  10/10 seeds 完全一致）
- recursion 從 day 12 到 day 27 一路 100%，我們 56~63%
- **收尾方向相反**：對手 day 28-29 把作物格從 63 收到 14 換現金；我們 day 29
  還有 25 格種在地裡收不回來 —— 那些種子和工時是純損失
- 最高同時在場工人 11 vs 14（⚠️ 期末只有 8 vs 10，`_late_crew_limit` 裁過。
  兩個數字不要混）

## 10. 🩸 掃了 20 組參數：**沒有一個能把作物格從 33 拉到 63，而且全部讓現金變差**

全部是 seeds 2000-2009 對 recursion 實測，不是推估。基準 cma1-g50 = 91,110 / 30.8 格。

```
                          我方現金   對手現金     margin   作物格
tiles_per_unit=4           81,084    123,588    −42,504    33.6
tiles_per_unit=5           77,667    118,969    −41,302    32.4
tiles_per_unit=6/7/9       84,192    126,051    −41,859    32.9  ← 三個值逐位元組相同
hands_per_quadrant=5       68,936     99,283    −30,347    30.9
max_hands=12 + hpq=5       72,178    102,372    −30,193    33.3
gen0 預設產能              63,425     97,408    −33,982    34.9
oversupply_factor=6        88,689    127,592    −38,903    30.7
oversupply_factor=20       88,777    127,793    −39,016    30.8  ← 等於解除限制，格數不動
osf20 + max_crop_share=1   65,317     91,967    −26,650    29.2
全開（所有上限拆掉）        80,520    115,534    −35,013    40.0
pri_PLANT 9→7/5/3/2/1     86,710~57,963          最好 −29,898  30.6→35.1
quadrant_zoning (4 組)     82,335~90,721         最好 −42,477  27.4→33.9
```

**把 34 個維度裡所有跟產能有關的上限全部拆掉，作物格最多 40，離對手的 63 還差
23 格，現金掉 10,590。**

排除掉的（都有實測）：地、`tiles_per_unit`、`max_hands`、`hands_per_quadrant`、
`oversupply_factor`、`max_crop_share`、種子、`_PRI` 優先序、`quadrant_zoning`。

### 順帶查出三個空轉的參數

CMA-ES 花了 68 代在調它們，對行為**零影響**：

1. `feed_crop_bootstrap_share`（0.5→0.077）和 `feed_crop_demand_weight`
   （1.0→0.399）—— 還原成預設之後**期末現金逐位元組相同**（91,110）。
   `agents/gen0.py:935-940` 加上去的 WHEAT 需求早就超過 `max_crop_share` 的
   上限，加更多也被切掉。
2. `hire_margin`（1.0→1.050）—— `agents/gen0.py:1939-1941`
   ```python
   hire_mult = float((config or {}).get("farmHandCostMult", 1))
   if hire_mult <= 1:
       return land_cap        # ← hire_margin 在這行之後才被讀到
   ```
   `farmHandCostMult` 本機預設 1，真實 episode `config/episodes/93783010.json`
   也是 1。**這條路永遠走不到。**

## 11. 真正的瓶頸：走路

`tools/tasks_probe.py`（包住 `agents/gen0.py:2353` 唯一的 `_assign` 呼叫，
3 局 2,157 個決策回合）：

```
unit 數                  9.36
閒置 unit 數             1.45   (16% 的 unit-回合沒事做)
完全沒有閒置的回合佔比    72%
              想做    做到    漏掉
全部任務      33.46    7.91   25.55
PLANT          5.39    0.75    4.64
手上種子總數  11.87            想種但種子=0 的回合 3 (0.1%) ← 種子不是瓶頸
PLANT 有漏掉的回合 1,591 (73.8%)
```

跟對手比動作組成（對手來自 episode 93848465 player1 的 log，我們是 3 局實跑）：

```
                    ReCurSiON    cma1-g50       差
走路小計               43.4%       57.2%    +13.8%   ←
PASS                   13.9%       12.8%     −1.1%
WATER                  15.2%        7.7%     −7.6%
HARVEST                 5.9%        4.2%     −1.7%
PLANT                   3.0%        1.9%     −1.1%
幹活小計               42.4%       29.9%    −12.5%
一回合幾個 unit          9.21        9.35
```

**人數幾乎一樣，但我們 57% 的 unit-回合在走路，對手 43%。實際幹活 29.9% vs
42.4%，差 1.42 倍**，跟產出差（作物格 33 vs 63、出貨量 900 vs 1,399）同量級。

`agents/gen0.py:1489` 的註解記著同一個問題修過一次：「第一版是按 (y, x) 掃描
順序……走路佔掉 74% 的回合」。74% → 57%，對手在 43%。

### 因果鏈

```
走路 57%（對手 43%）
  -> 每回合 33 件任務只做得到 7.9 件
  -> PLANT 是 _PRI 表最低的第 9 級，73.8% 的回合被擠掉
  -> 作物格停在 33（對手 63），常態空著 29 格
  -> 出貨量 900（對手 1,399）
  -> 期末現金差 38,085
```

### 三個控制點都不在 CMA 的 34 個維度裡

| | 現值 | 在 CMA 空間 |
|---|---|---|
| `_PRI` 優先序表（`PLANT: 9` 最低） | 模組常數 | 否 |
| `quadrant_zoning` / `zone_penalty` / `zone_planting_only` | `None`（關閉） | 否 |
| `optimal_assignment` | `True`，但 `gen0.py:1533` 排除含 PLANT 的層 | 否 |

最後一項特別可疑：全域最短配對**只在該優先層沒有 PLANT 時才啟用**，而 PLANT
自己就是一整層。唯一沒享受到路徑最佳化的，正好是被擠掉最多的那個任務。

## 12. 🩸 判準：`margin` 和「我方現金」會系統性打架

§10 的表裡到處是這個：`pri_PLANT=3` 的 margin −29,898 比基準 −38,085 **好
8,187**，但我方現金 57,963 比基準 91,110 **差 33,147**（兩敗俱傷，只是輸得少）。

`tools/param_search.py:132-136` 早就標記過原因：

> 🩸 `margin` 對開迴路 replay 有一個誘因問題：壓低對手的現金可以靠讓它的
> `BUY_*` / `HIRE` 失敗達成，那是在利用 replay 不會反應的弱點。

**所以「我方現金」和「margin」都不是判準，勝率才是**（Kaggle 用勝負算 rating）。
本 session 有好幾次來回是被這件事絆住的 —— 下次先決定判準再掃。

⚠️ **`python -m eval.runner --ladder` 不帶路徑會炸**：預設吃
`config/ladder.json`，那是舊的開發對手池（pass / random / starter / ref-v* /
gen0 變體），裡面的 `ref-v2` 已經不存在。真正的 8 支頂端玩家在
`config/ladder-top.json`，所以要寫

    python -m eval.runner --a <spec> --ladder config/ladder-top.json

`config/ladder-top.json` 的 8 個名字跟 `tools/param_search.py` 的
`HOLDOUT_TEAMS` 前 8 項一致（第 9 項 `gen1` 不在裡面）。

## 13. A 和 B 都測了，**七個方向全部證偽**

判準先講清楚（§12 的教訓）：**勝率**是唯一判準，`tools/param_search.py` 的
`HOLDOUT_TEAMS` 8 支、配對種子正反手。`我方現金` 和 `margin` 只拿來當機制的
指示器，不當判準。

```
                        走路     幹活    期末現金   作物格   ladder 勝率
對手 ReCurSiON         43.4%   42.4%    129,195     63        —
cma1-g50 基準          57.2%   30.0%     91,110    30.8     20.6%
pri_PLANT=3            —        —        57,963    34.6     17.5%
全域 PLANT 配對        56.6%   30.3%     90,843    30.8       —
密集 active tiles      60.3%   28.5%     89,725    25.7       —
```

（走路 / 幹活是 3 局、約 20,200 個 unit-回合；現金是 seeds 2000-2009 對
recursion 10 局；勝率是 8 支 × 10 個配對種子 = 160 局。⚠️ 基準這裡量到
20.6%，跟 §5 記的 14.4% 不同 —— 那次是 640 局、不同 seed 範圍，**不要混用**。）

| 假設 | 變體數 | 結果 |
|---|---|---|
| 地買太少 | — | 兩邊都 75 格，買地時機差 1 天（10/10 seeds 一致） |
| 產能上限太緊 | 10 | 全部讓現金變差；所有上限拆掉作物格最多 40（對手 63） |
| 種子不夠 | — | 想種但沒種子的回合 3 / 2,157（0.1%） |
| `_PRI` 的 PLANT 太低 | 5 + ladder | 勝率 20.6% → 17.5%，沒有改善 |
| `quadrant_zoning` | 4 | 全部更差 |
| PLANT 沒吃到全域最短配對 | 1 | 走路 −0.6pp、現金 −267，等於沒動 |
| active tiles 太分散 | 1 | 走路 **+3.1pp**，更差 |

### 為什麼全域 PLANT 配對沒用

`PLANT` 是最後一層（`_PRI` = 9），輪到它時 `free` 平均只剩 **1.48** 個 unit，
待辦 PLANT 有 5.24 個。1.5 個 unit 配 5 個任務，「最近的先配」和「全域最佳配對」
算出來幾乎是同一個答案 —— **沒有最佳化空間**。問題在上游：unit 在更高優先序的
層就被吃光了。

### 為什麼密集配置反而更糟

`agents/gen0.py:1500-1503`：

> unit 0（farmer）固定從 NW 開始，其餘依 index 輪流分區。**hands 每天也是
> NW/NE/SW/SE 內角輪流出生**

**工人本來就是跨象限輪流生出來的**，所以 `active_crop_tiles:384` 的 round-robin
不是在浪費效率，它是在配合工人的分布。把工作集中到一個象限 = 另外兩個象限出生的
工人每天走半張圖上班。兩個 round-robin 是耦合的，不能只動一邊；而「工作集中 +
工人也集中」就是 `quadrant_zoning`，測過，更差。

### 自我維持的迴圈（機制成立，但上面七個都不是入口）

```
使用率 61%（29 格空著）
  -> unit 走過的格子 39% 是空的，走了沒事做
  -> 走路吃掉 57% 的 unit-回合
  -> 每回合只做得到 7.9 / 33.5 件事
  -> PLANT（最低優先）被擠掉 73.8%
  -> 使用率上不去  ← 回到開頭
```

這解釋了為什麼「單純加產能」的 10 組全部變差：**在同樣稀疏的盤面上多開工地，
unit 走更遠、每件事做得更少。**

## 14. 新增的工具

```
新  tools/gen0_pri.py     不動 agents/gen0.py 的前提下換掉 gen0 的三處行為：
                          pri_<動作名>            覆寫 _PRI 那一項
                          global_plant_assignment PLANT 層也走全域最短配對
                          dense_active_tiles      先填滿最近的象限
                          🩸 用 inspect.getsource + 字串替換 + exec 做的，
                             B 改了 _assign 會在 import 時 assert 失敗，
                             不會安靜地跑舊行為
```

⚠️ **`tools/gen0_pri.py` 是實驗用的，三個開關量出來都沒有正面效果**，
現在沒有任何 spec 在用。留著是因為那套「不動 B 的檔案就能換掉 gen0 內部函式」
的機制下次還用得到。要清掉也可以。

## 15. 🔴 上榜炸了：`ModuleNotFoundError: No module named 'contracts'`

`submission/cma1-g50/` 送上 Kaggle 之後每一回合都拋：

```
File "/kaggle_simulations/agent/gen0.py", line 48, in <module>
    from contracts import TASK_OPS, target_xy
ModuleNotFoundError: No module named 'contracts'
```

### 根源不只是我手工打包漏了

`serving/build_submission.py` 的 `FILE_MAP` 從 2026-08-21 起就只有兩項：

```python
FILE_MAP = {"main.py": "main.py", "agents/gen0.py": "gen0.py"}
```

註解寫「規則式只要兩支檔案」。但 `agents/gen0.py:48` 有
`from contracts import TASK_OPS, target_xy`。**這支工具產出的包本身就是壞的**，
`submission/main.py` + `submission/gen0.py` 同樣缺 `contracts.py`。

### 🩸 為什麼 640 局的驗收完全沒抓到

`config/opponents/cma1-g50-sub.json` 走 `builtin` 分支，是在 **repo root 底下**
跑的：`contracts.py` 就躺在 root 上，cwd 又在 `sys.path` 裡，那個 import 一定成功。
Kaggle 上只有 `/kaggle_simulations/agent/` 那個目錄。

**「本機跑得動」對「少放檔案」這一類錯誤鑑別力是零。** §5b 說「驗到最嚴格標準、
640/640 逐位元組相同」是錯的 —— 逐位元組相同只證明兩邊參數一致，跟打包完整性
無關。

### 兩層修法（都做了負控制）

1. **靜態**：`tests/test_submission_package.py::
   test_every_packaged_file_only_imports_packaged_or_external`
   —— 遞迴掃每個打包檔的模組層 import，只要名字在 repo root 有對應的
   `.py` / package 卻沒打包就紅。0.1 秒。
   🩸 判斷用「repo root 有沒有這個檔案」，**不能用 `find_spec`** ——
   跑測試時 repo root 在 `sys.path` 上，`find_spec("contracts")` 一定找得到。
   既有的 `test_main_entry_matches_what_is_packaged` 只看 `main.py` 自己那幾行，
   看不到 `gen0.py` 又 import 了什麼，所以漏掉。

2. **執行期**：`tools/submission_check.py`
   ```
   python -m tools.submission_check submission/cma1-g50
   ```
   把目錄複製到 repo 外的暫存目錄，開一個 cwd 在那裡、`sys.path` 不含 repo root
   的**子行程**跑完整局。
   🩸 一定要子行程 —— 同行程裡 `contracts` 早就在 `sys.modules` 了，改
   `sys.path` 擋不掉。
   負控制：拿掉 `contracts.py` 會重現一模一樣的 Kaggle 錯誤訊息。

### 現況

```
serving/build_submission.py   FILE_MAP 補上 contracts.py
submission/contracts.py       補（榜上那版的來源也缺）
submission/cma1-g50/contracts.py  補
submission/cma1-g50/submission.zip  重做，3 支檔案

python -m tools.submission_check submission/cma1-g50   -> PASS  期末 143,898
python -m tools.submission_check submission            -> PASS  期末 135,366
pytest 114 passed
```

## 16. 本機 ladder 勝率對照（同一批設定才能比）

8 支 `config/ladder-top.json` × 10 個配對種子正反手 = 160 局，`tools/` 底下的
`ladder_eval.py`（在 scratchpad，沒進版控）：

```
                     勝率     我方現金    對手現金   現金比
  submission（榜上）   3.8%     69,907     121,385   57.6%
  gen1                3.8%     69,907     121,385   57.6%   ← 每個數字都相同
  cma1-g50           20.6%     73,805      92,760   79.6%

  勝率差 +16.8pp   標準誤 3.5pp   z = 4.8   95% 區間 +9.9 ~ +23.7pp
```

**榜上跑的就是 gen0 預設參數**（`submission` 和 `gen1` 逐位元組相同），
CMA 那 68 代還沒上過場。

⚠️ 這 8 支是開迴路 replay，§5 量到對 replay 的改善有 81% 來自壓低對手、
對會反應的對手只有 32%。真實榜上的增幅會**明顯小於 16.8pp**。

## 17. 🔴 整回合全域配對：走路真的降了，但**因果方向是反的**

`agents/gen0.py` 新增 `whole_turn_assignment`（預設關）：把「照優先序一層一層
發、人用完就停」換成**一次全域最短配對**，優先序折成成本
（`priority_step_cost`：高一級 = 多走 N 步）。

```
                  走路     幹活     PASS   每回合做到  閒置unit   期末現金
cma1-g50 基準    57.2%   30.0%   12.8%      7.91      1.45     91,110
整回合 step=1    49.8%   32.1%   18.1%      7.16      2.20     89,752
整回合 step=3    55.0%   31.2%   13.8%       —         —       79,819
整回合 step=20   56.5%   30.9%   12.6%       —         —       65,243
對手 ReCurSiON   43.4%   42.4%   13.9%       —         —      129,195
```

**走路降了 7.4pp（一半以上的差距），但省下來的變成 PASS，不是產出。**
幹活只 +2.1pp，每回合做到的事**變少**（7.91 → 7.16），閒置的人**變多**。

所以 §11 那條因果鏈是反的：

```
❌ 走路多 -> 做不完 -> 種田被擠掉 -> 地空著
✅ 地空著 -> 走過去的格子沒事做 -> 走路多
```

對手走路少不是排程比較聰明，是**它 75 格全種滿，走到哪裡都有活幹**。
我們把排程改成「不要走遠」，人就站著不動 —— 遠方本來就沒有更值錢的事。

## 18. 🩸 更正 §11：「種子不是瓶頸」是錯的

§11 量的是「手上種子**總數** = 0」的回合（3/2157，0.1%）。但
`agents/gen0.py:1578` 擋的是**逐作物**：

```python
if planted.get(crop, 0) >= seeds.get(crop, 0): continue
```

總數不是 0，完全不代表想種的那一種有種子。重新逐作物量（3 局 2,157 回合）：

```
想種的格子           5.39
  其中缺種子         1.87   (35%)
  其中有種子         3.51
實際指派出去         0.75
同時閒置的 unit      1.45
```

**35% 的種植需求死在種子上**，不是 0.1%。

但那是症狀不是原因 —— `seed_buffer_days` 從 2 拉到 4 / 6 / 10：

```
buf=4   89,429   30.9 格      buf=6   67,486   30.3 格
buf=10  72,557   30.2 格      buf=6 + tpu=5  73,967  32.1 格
```

現金全掉、作物格完全不動。種子預算本來就是照「實際種得完多少」抓的。

## 19. 今天的總結：gen0 在一個真的局部最優上

`可種格數`（`basket_probe`，seed 2000）從 day 9 起卡在 **36**
= `(1 + planned_hands) × tiles_per_unit` = `(1+11) × 3`。WHEAT 每天都是 10 格
= `int(36 × 0.287)`，`max_crop_share` 的上限。

但把上限拉到 72（`tiles_per_unit=6`）：種子只從 127 顆 -> 145 顆（+14%），
**作物格 32.9，一格沒多**。

今天總共測了 **九個方向、約 30 個變體**，全部實測（seeds 2000-2009 對
recursion，另加 480 局 ladder 勝率）：

| 方向 | 結果 |
|---|---|
| 地 | 兩邊都 75 格，時機差 1 天 |
| 產能上限（tiles_per_unit / 人數 / oversupply / max_crop_share） | 全部更差；上限全拆作物格最多 40 |
| `_PRI` 的 PLANT 優先序 | ladder 勝率 20.6% -> 17.5% |
| `quadrant_zoning` | 全部更差 |
| PLANT 層走全域配對 | 走路 −0.6pp，等於沒動 |
| 密集 active tiles | 走路 **+3.1pp**，更差 |
| **整回合全域配對** | 走路 **−7.4pp**，但變成 PASS，現金 −1,358 |
| **逐作物種子** | 35% 需求被擋，但放寬預算現金全掉 |
| 飼料作物 / hire_margin | 空轉參數，行為零影響 |

**沒有一個能把作物格從 33 推到 63，也沒有一個能把現金推過 91,110。**

### 這代表什麼

差別不在執行效率，在**經濟判斷**。同樣 75 格：

- 對手撒 143 顆便宜 WHEAT 種子（$1,430），收 447 個賣掉（$20,753），
  **超產 447 個，均價只從 $49.9 掉到 $46.4** —— 幾乎沒掉
- 我們的 `_plan_basket` 只配 33 格，WHEAT 10 格封頂

`_plan_basket` 的 docstring 自己就寫著「WHEAT / EGG 這種 `above_func = log`
的，超產也幾乎不掉價 → 一直有價值」。**模型知道這件事，但配置沒有反映它。**
下一個要查的就是這個矛盾 —— 那是一個函式，不是一個架構。

### 對 self-play 的判斷

**現在不是時機。** 距離 2026-09-30 截止還有 33 天，一次 RL 嘗試 5.4 天
（journal §6），時間塞得下。但：

- 網路目前比 gen0 弱（使用者確認）
- 模仿學習是**拿 gen0 當老師**的，連老師都追不上
- 我們連「多種 30 格會不會賺」都還沒搞懂，RL 要在 18^9 的聯合動作空間裡
  自己摸出來

決定點：**如果要開 self-play，不能拖過 2026-09-07** —— 一次 5.4 天，
拖到 9 月中就只剩一次機會，等於沒有。

## 7. 今天改的檔案

```
新  config/params/cma1-g50.json      CMA 第 50 代，51 項完整展開（出貨用這組）
新  config/params/cma1-g68.json      第 68 代，對照
新  config/opponents/cma1-g50-sub.json  驗打包用的 builtin spec
新  model/cma1-g50.pkl / cma1-g68.pkl   CMA 狀態各約 310 KB（--warm-start / 重現）
改  model/README.md                  補一節說明那兩個 pkl（🩸 B 的檔案，要告知）
新  submission/cma1-g50/             打包好的：main.py（內嵌 PARAMS）+ gen0.py
改  tools/param_search.py   --watch（後來修兩次：漏掉 holdout 那一欄；
                            歷來最佳沒更新時連續 checkpoint 相同、
                            「train 升但其它全退」永遠不會觸發）
                            --warm-start；--log-level 預設 0；
                            HOLDOUT_TEAMS 3 支 -> 8 支 + gen1；
                            load_team_specs 找不到就退回 load_spec
新  tools/_quiet.py         import kaggle_environments 時 open_spiel 會噴 336 行
                            到 stderr（fd 層級，redirect_stderr 攔不到）
改  tools/param_space.py    :47 直接 import 引擎 -> param_search / noise_probe /
                            freeze_params 全被牽連
改  tools/revenue.py        自帶的 _silenced 改成共用 tools/_quiet.py
改  tools/freeze_params.py / market_calibrate.py / prior_eval.py /
    state_dist.py / search_finetune_probe.py    同樣包起來
```

`pytest` 113 項全過。

🩸 **驗收會寫爆磁碟**：一輪 640 局的逐局 log 是 **661 MB**（`--log-level` 預設
不是 0）。跑驗收記得加 `--log-level 0`。
