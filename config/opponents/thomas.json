{
  "name": "thomas",
  "entry": "agents.replay:act",
  "params": {
    "episode": "94019142",
    "player": 1
  },
  "note": "rating 3162.6。跟 kawashigi 同一局的另一邊。 從 config/ladder-top.json 抽出來成獨立 spec，讓 eval.runner.load_spec 叫得到（它只找 config/opponents/<name>.json）。⚠️ 開迴路 replay，細節見 agents/replay.py docstring。"
}
