{
  "name": "animal-sweep",
  "note": "只改動物那一邊，作物一律用動態 basket。第一版把 dynamic_basket 一起關掉，量到的是兩者混在一起的結果。 ⚠️ 2026-08-21 更新：(1) `agents/gen1.py` 併入 `agents/gen0.py`，`gen0.DEFAULT_PARAMS` 多了 gen1 的 21 項（n_structures 9->12、optimal_assignment False->True、tiles_per_unit、water_on_demand 等）。**這個 sweep 內部仍然可比**（所有變體共用同一組新預設），但**不能拿現在的數字跟 2026-08-16~19 那批比**。 (2) Gen0 線的 9 個對手已刪除（ref-v1 / ref-v2 / gen0 / 5 個 gen0-* / pq4）。固定 A 方請改用 `ref-v11`（唯一把 51 項參數完整展開的，不會隨預設值飄）。",
  "opponents": [
    {
      "name": "goose-only",
      "entry": "agents.gen0:act",
      "params": {
        "dynamic_animals": false,
        "animal": "GOOSE"
      }
    },
    {
      "name": "cow-only",
      "entry": "agents.gen0:act",
      "params": {
        "dynamic_animals": false,
        "animal": "COW"
      }
    },
    {
      "name": "sheep-only",
      "entry": "agents.gen0:act",
      "params": {
        "dynamic_animals": false,
        "animal": "SHEEP"
      }
    },
    {
      "name": "no-animals",
      "entry": "agents.gen0:act",
      "params": {
        "n_structures": 0
      }
    },
    {
      "name": "structures-3",
      "entry": "agents.gen0:act",
      "params": {
        "n_structures": 3
      }
    },
    {
      "name": "structures-10",
      "entry": "agents.gen0:act",
      "params": {
        "n_structures": 10
      }
    },
    {
      "name": "structures-14",
      "entry": "agents.gen0:act",
      "params": {
        "n_structures": 14
      }
    },
    {
      "name": "dynamic",
      "entry": "agents.gen0:act",
      "params": {}
    }
  ]
}
